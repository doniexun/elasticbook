<h1>Results</h1>
{{range .}}
  <div> {{.Title}} - {{.Url}} ({{.Score}})</div>
{{ end }}

<h1>Search</h1>
<form action="/elasticbook/search" method="POST" class="pure-form">
  <input type="text" name="name" placeholder="name" />
  <input type="submit" value="submit" class="pure-button pure-button-primary"/>
</form>
